/*CREATE TABLE `la_haus`.`properties` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `title` VARCHAR(115) DEFAULT '' NOT NULL,
  `description` VARCHAR(115) NULL,
  `latitude` DOUBLE DEFAULT 0 NOT NULL,
  `longitude` DOUBLE DEFAULT 0 NOT NULL,
  `sale_price` INT DEFAULT 0 NOT NULL,
  `administrative_fee` INT,
  `property_type` ENUM("HOUSE", "APARTMENT") NOT NULL,
  `bedrooms` INT UNSIGNED DEFAULT 0 NOT NULL,
  `bathrooms` INT UNSIGNED DEFAULT 0 NOT NULL,
  `parking_spots` INT UNSIGNED NULL,
  `area` INT UNSIGNED DEFAULT 0 NOT NULL,
  `photos` VARCHAR(115) NULL,
  `created_at` VARCHAR(115) NULL,
  `updated_at` VARCHAR(115) NULL,
  `status` VARCHAR(115) NULL,
  PRIMARY KEY (`id`),
  UNIQUE INDEX `id_UNIQUE` (`id` ASC) VISIBLE);*/